# Docx-Table-Processing
Python script for table processing in a word docx 

### Current State
- currently only working with rows but columns can be hardcoded

### Pre-requisites
- python-docx Library
    _'pip install python-docx'
